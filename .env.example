# MySQL
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=tarkov_board
MYSQL_USER=tarkov
MYSQL_PASSWORD=change_me_db_password
TZ=Asia/Shanghai

# App auth
APP_ADMIN_USERNAME=admin
# Optional. If not set, docker-compose.yml fallback hash will be used.
# APP_ADMIN_PASSWORD_HASH=$2a$10$replace_with_bcrypt_hash

# JWT
APP_JWT_SECRET=replace_with_a_strong_random_secret
APP_JWT_EXPIRESECONDS=7200

# Deploy image tag from GHCR (e.g. v1.5.4 / latest)
APP_IMAGE_TAG=latest
